use_module(library(dialect/sicstus/timeout)).
consult('setlog.pl').
consult_lib.
set_prolog_flag(toplevel_print_options, [quoted(true), portray(true)]).
get_time(Tini).
setlog(
% SET067+1.p
% If one argument is a proper class, pair contains only the other
nsubset({X,X},{X,Y})

,2000,_CONSTR,_RES).

get_time(Tend).


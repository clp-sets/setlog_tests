use_module(library(dialect/sicstus/timeout)).
consult('setlog.pl').
consult_lib.
set_prolog_flag(toplevel_print_options, [quoted(true), portray(true)]).
get_time(Tini).
setlog(
% SET229-6.p
% Cross product right cancellation 2
cp(U,V) = cp(W,X) & X neq {} & U neq W


,2000,_CONSTR,_RES).

get_time(Tend).


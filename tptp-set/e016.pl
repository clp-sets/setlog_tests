use_module(library(dialect/sicstus/timeout)).
consult('setlog.pl').
consult_lib.
set_prolog_flag(toplevel_print_options, [quoted(true), portray(true)]).
get_time(Tini).
setlog(
% SET016+1.p
% First components of equal ordered pairs are equal
[W,X] = [Y,Z] & W neq Y

,2000,_CONSTR,_RES).

get_time(Tend).


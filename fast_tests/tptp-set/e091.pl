use_module(library(dialect/sicstus/timeout)).
consult('setlog.pl').
consult_lib.
set_prolog_flag(toplevel_print_options, [quoted(true), portray(true)]).
get_time(Tini).
setlog(
% SET138-6.p
% Kludge 2 to instantiate variables in unordered triples
U nin {U/{V/{W/{}}}}


,2000,_CONSTR,_RES).

get_time(Tend).


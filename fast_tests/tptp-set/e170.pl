use_module(library(dialect/sicstus/timeout)).
consult('setlog.pl').
consult_lib.
set_prolog_flag(toplevel_print_options, [quoted(true), portray(true)]).
get_time(Tini).
setlog(
% SET216-6.p
% Corollary 7 to cross product product monotonicity
inters(Y,Z,M1) & nsubset(cp(X,M1),cp(X,Y))

,2000,_CONSTR,_RES).

get_time(Tend).


use_module(library(dialect/sicstus/timeout)).
consult('setlog.pl').
consult_lib.
set_prolog_flag(toplevel_print_options, [quoted(true), portray(true)]).
get_time(Tini).
setlog(
% SET186-6.p
% Subclass property 4
un(X,Y,Y) & nsubset(X,Y)

,2000,_CONSTR,_RES).

get_time(Tend).


use_module(library(dialect/sicstus/timeout)).
consult('setlog.pl').
consult_lib.
set_prolog_flag(toplevel_print_options, [quoted(true), portray(true)]).
get_time(Tini).
setlog(
% BOO026-1.p
% Absorption from self-dual independent 2-basis
% pixley3_dual axiom
pixleyd(X,Y,X,U,M1) & M1 neq X


,2000,_CONSTR,_RES).

get_time(Tend).

